/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "default_image.jpg",
      "127.0.0.1",
      "localhost",
    ],
  },
};

export default nextConfig;
